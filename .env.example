MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.a4pigaw.mongodb.net/?retryWrites=true&w=majority&authSource=admin&appName=Cluster0
MONGODB_DB=guestmatrix

# On Vercel: Settings → Environment Variables → add both for Production, Preview, and Development, then Redeploy.
# Paste the URI with no quotes and no <db_username> placeholder.
# In Atlas: Network Access → allow 0.0.0.0/0 so Vercel can connect.


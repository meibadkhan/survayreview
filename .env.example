MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.a4pigaw.mongodb.net/?appName=Cluster0
MONGODB_DB=guestmatrix

# On Vercel: Settings → Environment Variables → add both for Production, then Redeploy.
# In Atlas: Network Access → allow 0.0.0.0/0 so Vercel can connect.

